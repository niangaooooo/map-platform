# 前端构建配置模板：复制为 .env.local 并填写真实值
# （.env.local 不入库；Vite 构建时会把下列变量注入 index.html 的 amap-key / amap-security-code meta 标签）

# 高德开放平台 → 应用管理 → JS API Key
# 注意：需在高德控制台为该 Key 配置「域名白名单」（如 https://your-domain.com、http://localhost:8000），
# 否则浏览器控制台会报 INVALID_USER_KEY 且地图不显示。
VITE_AMAP_KEY=your-amap-js-api-key

# JS API 安全密钥（控制台开启「安全密钥」后才有；未开启可留空）
VITE_AMAP_SECURITY_CODE=your-amap-security-code
